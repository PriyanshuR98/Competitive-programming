#include<bits/stdc++.h>
using namespace std;


int  main()
{
    int a,b;
    cin>>a>>b;
    
    for(int g=b;g>=1;g--)
    {
        if((b/g)-((a-1)/g)>=2)
        {
            cout<<g<<endl;
            break;
        }
    }

}
